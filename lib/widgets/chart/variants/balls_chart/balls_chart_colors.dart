import 'dart:ui';

class BallsChartColors {
  static const Color blackBalls = Color(0xFF000000);
  static const Color whiteBalls = Color(0xFFFFFFFF);
  static const Color randomBalls = Color(0xFF0000FF);
  static const Color currentBalls = Color(0xFF00FF00);
  static const Color enemyBalls = Color(0xFFFF0000);
}
